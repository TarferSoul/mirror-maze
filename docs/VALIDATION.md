# 验证记录

验证日期：2026-09-16。

## 自动化验证

执行 `npm run check`：25 项测试全部通过，静态构建成功。

覆盖范围：

- 撞墙、越界、非法动作不会改变状态或步数。
- 钥匙不会重复计数，切换世界可以收集目标格钥匙。
- A/B 机关与门独立联动；机关开启状态跨世界保留。
- 未收齐钥匙时不能通关；通关后不再接受游戏动作。
- 状态转换不修改历史快照，支持准确撤销。
- BFS 返回已知最短路线，不可达时返回 `null`。
- 星级评价在阈值边界上符合规则。
- 五个关卡均通过最短路线验算和全部可达状态的无死局检查。
- 存档缺失、损坏、版本不符、计数非法、存储不可用时安全退化。
- 关卡连续解锁，最佳记录不会被更差成绩覆盖。

## 浏览器操作验收

在实际浏览器中通过方向键与空格依次完成五关，未直接修改游戏状态：

| 关卡     | 移动 | 切换 | 总步数 | 结果                         |
| -------- | ---- | ---- | ------ | ---------------------------- |
| 初次穿越 | 12   | 2    | 14     | 3 星通关                     |
| 镜中岔路 | 12   | 4    | 16     | 3 星通关                     |
| 遗落的光 | 12   | 2    | 14     | 3 星通关，钥匙正常收集       |
| 共鸣机关 | 16   | 2    | 18     | 3 星通关，A 机关正常联动     |
| 两界归一 | 24   | 4    | 28     | 3 星通关，双钥匙与双机关正常 |

另外检查了：

- 首次开始菜单与初始关卡锁定。
- 撞墙反馈、步数不变、提示文字与目标格高亮。
- 移动后撤销，坐标和步数恢复；重来后恢复初始状态。
- 通关成绩弹窗、下一关、最后一关结局、返回第一关。
- 刷新后保留 5/5 完成记录、关卡解锁和最佳成绩；当前局面重置。
- 玩法说明打开时方向键不移动角色，Esc 正常关闭。
- 音效开关状态变化正常，操作后未记录到浏览器错误。
- 390 × 844 布局下，通过屏幕按钮完整完成第五关。
- 390 和 320 像素宽度均无横向溢出；9 × 9、11 × 9 地图保持正确格子比例。
- 1280 像素桌面宽度下，双世界预览、主地图和操作区域正常显示。

## 验证边界

浏览器操作验收是在桌面浏览器与模拟手机尺寸下完成，未声称覆盖实体手机或所有浏览器版本。自动化测试直接验证规则与存档模块；浏览器验收通过实际界面操作完成。音效验证覆盖开关和无异常，不包含扬声器音质测量。
